# Be sure to restart your server when you modify this file.

# Your secret key is used for verifying the integrity of signed cookies.
# If you change this key, all old signed cookies will become invalid!

# Make sure the secret is at least 30 characters and all random,
# no regular words or you'll be exposed to dictionary attacks.
# You can use `rake secret` to generate a secure secret key.

# Make sure the secrets in this file are kept private
# if you're sharing your code publicly.

development:
  # admin_name: First User
  # admin_email: user@example.com
  # admin_password: changeme
  # email_provider_username: <%= ENV["GMAIL_USERNAME"] %>
  # email_provider_password: <%= ENV["GMAIL_PASSWORD"] %>
  domain_name: example.com
  secret_key_base: 423c97077d514e9994ba3c31f2000721048ff0adf3fd79909dc551241a95184d6ac959b568f4b2fe0b8d20040c7eb1a233a35e7d82c086c2719c17dc73656f15

test:
  secret_key_base: 1a87077544ac2ed5a6ccece8cef013a59bc8c6d045e7104c19be05c9ec5b370b00582802ccf0124c35aa647ff4a7d8eb11be8bfa2088792746b465a06b7dc39f

# Do not keep production secrets in the repository,
# instead read values from the environment.
production:
  secret_key_base: <%= ENV["SECRET_KEY_BASE"] %>


